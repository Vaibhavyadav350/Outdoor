import 'package:flutter/cupertino.dart';

class EditStays extends StatelessWidget {
  const EditStays({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
