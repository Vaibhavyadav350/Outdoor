import 'package:flutter/cupertino.dart';

class Postpone extends StatelessWidget {
  const Postpone({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
