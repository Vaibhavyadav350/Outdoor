import 'package:flutter/cupertino.dart';

class EditTrips extends StatelessWidget {
  const EditTrips({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
